const username: string = 'JuanBF';

const sum = (a: number, b: number) => {
  return a + b;
}

sum(1,3);

class Person {

  constructor(public age:number, public lastName: string){}
}

const juan = new Person(15, 'Barrios');

